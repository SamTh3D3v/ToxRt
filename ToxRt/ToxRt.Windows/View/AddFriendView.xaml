<helpers:BindablePage
    x:Class="ToxRt.View.AddFriendView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ToxRt.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:ToxRt.Helpers"
    mc:Ignorable="d">
    <helpers:BindablePage.DataContext>
        <Binding Source="{StaticResource Locator}" Path="AddFriendViewModel"></Binding>
    </helpers:BindablePage.DataContext>

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border  BorderThickness="1" BorderBrush="WhiteSmoke" Grid.Row="0" Margin="5">
            <TextBlock Text="Send a friend request" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="20"/>
        </Border>
        <Border BorderThickness="1" BorderBrush="WhiteSmoke" Grid.Row="1" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="ToxId" Grid.Row="0" Grid.Column="0" FontSize="20"/>
                <TextBox Margin="2" Grid.Column="1" Grid.Row="0"></TextBox>

                <TextBlock Text="Message" Grid.Row="1" Grid.Column="0" VerticalAlignment="Top" FontSize="20"/>
                <TextBox AcceptsReturn="True" Height="150" Margin="2" Grid.Column="1" Grid.Row="1" VerticalAlignment="Top"></TextBox>
            </Grid>
        </Border>
        <Border BorderThickness="1" BorderBrush="WhiteSmoke" Grid.Row="2" Margin="5">
            <TextBlock Text="Panding request" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="20"/>
        </Border>
        
    </Grid>
</helpers:BindablePage>
